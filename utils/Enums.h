#pragma once

enum class GameState {
    START,
    PLAYING,
    PAUSED,
    GAME_OVER
};

enum class Difficulty {
    EASY,
    NORMAL,
    HARD
};

enum class Theme {
    FOREST,
    DUNGEON,
    SPACE
};
